binary search
leetcode 704
```python
def bs(nums):
  left, right = 0, len(nums)
  while left <= right:
    mid = (left+right)//2
    if nums[mid]==right:
      return nums[mid]
    elif: nums[mid]< target:
      left = mid
    else:
      right = mid
```
